<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
      Window background is black so the activity-load flash matches the
      all-black Compose UI. Without this, the default Material.Light theme
      paints the window white for a frame on cold start.
    -->
    <style name="Theme.MemoryLights" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
